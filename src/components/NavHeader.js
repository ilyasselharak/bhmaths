'use client';
// NavHeader is now a thin visual accent bar — full nav lives in MainHeader.
// This component is kept for visual brand continuity (orange underline bar).
export default function NavHeader() {
  return null;
}
